---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: objectstorages.storage.example.io
spec:
  group: storage.example.io
  names:
    kind: ObjectStorage
    listKind: ObjectStorageList
    plural: objectstorages
    singular: objectstorage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ObjectStorage is the Schema for the ObjectStorage.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObjectStorageSpec defines the desired state of ObjectStorage.
            properties:
              region:
                description: |-
                  Region is the geographic region where the object storage should be created.
                  The resource-broker routes the order to a provider that accepts this region.
                type: string
              versioning:
                description: Versioning enables object versioning.
                type: boolean
            type: object
          status:
            description: ObjectStorageStatus defines the observed state of ObjectStorage.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the ObjectStorage's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              protocol:
                description: Protocol is the object storage API dialect the endpoint
                  speaks.
                enum:
                - S3
                - Azure
                - GCP
                type: string
              relatedResources:
                additionalProperties:
                  properties:
                    gvk:
                      description: GroupVersionKind unambiguously identifies a kind.
                      properties:
                        group:
                          type: string
                        kind:
                          type: string
                        version:
                          type: string
                      required:
                      - group
                      - kind
                      - version
                      type: object
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - gvk
                  - name
                  type: object
                description: |-
                  RelatedResources lists resources related to this ObjectStorage, keyed by identifier.
                  The resource-broker copies these (e.g. a result Secret) back to the consumer.
                type: object
              status:
                description: Status is a human-readable provisioning state (e.g. Provisioning,
                  Available), set by the realizing provider.
                type: string
              url:
                description: URL is the address of the provisioned object storage
                  (e.g. gs://bucket or s3://bucket), set by the realizing provider.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
